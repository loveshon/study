Day 1 배열의 삭제

var = nums[]10, 20, 30, 40, 50} 에서 40, 50 삭제

pop()를 사용하면 배열 마지막 값이 삭제됨. 다만 두 번을 사용해야하기에 제외함
splice() 로 삭제할 배열의 시작 위치를 지정 (인덱스) 하고 얼마나 삭제할지를 (개수) 요소로 넣어줌

nums.splice(3, 2)

정답은 pop() 두 번 사용하고 있었음
